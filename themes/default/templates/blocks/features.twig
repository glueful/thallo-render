<section class="thallo-block thallo-block-features thallo-block-features--cols-{{ data.columns|default('3') }}">
  <div class="thallo-block-features__inner">
    {% if data.title or data.intro %}
    <div class="thallo-block-features__header">
      {% if data.title %}<h2 class="thallo-block-features__title">{{ data.title|editable_text('title') }}</h2>{% endif %}
      {% if data.intro %}<p class="thallo-block-features__intro">{{ data.intro|editable_text('intro') }}</p>{% endif %}
    </div>
    {% endif %}
    <div class="thallo-block-features__grid">{{ blocks(data.items) }}</div>
  </div>
</section>
