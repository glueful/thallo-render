<section class="thallo-block thallo-block-faq">
  <div class="thallo-block-faq__inner">
    {% if data.title %}<h2 class="thallo-block-faq__title">{{ data.title|editable_text('title') }}</h2>{% endif %}
    <div class="thallo-block-faq__items">
      {# Items rendered inline: <details name> needs the PARENT faq's group id
         for native exclusive-open — a child template can't know it. #}
      {% for item in data.items|default([]) %}
      <details class="thallo-block-faq__item"{% if not data.multiple|default(false) %} name="faq-{{ block.id }}"{% endif %}>
        <summary class="thallo-block-faq__question">{{ item.data.question|default('') }}</summary>
        <div class="thallo-block-faq__answer">{{ item.data.answer|default('')|safe_html }}</div>
      </details>
      {% endfor %}
    </div>
  </div>
</section>
