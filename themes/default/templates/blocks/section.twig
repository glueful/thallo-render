<section class="lemma-block lemma-block-section lemma-block-section--{{ data.background|default('none') }}">
  <div class="lemma-block-section__inner">
    {% if data.title %}<h2 class="lemma-block-section__title">{{ data.title|editable_text('title') }}</h2>{% endif %}
    {{ blocks(data.content) }}
  </div>
</section>
