{# social_links — a row of social_link children. Fields: items (child blocks).
   Layout (flex row + gap) lives in .thallo-block-social_links in assets/blocks.css. #}
<div class="thallo-block thallo-block-social_links{{ style_classes('root') }}"{{ style_attrs('root') }}{{ slot_attrs('items') }}>{{ blocks(data.items) }}</div>
