{# Raw by DESIGN (block-library spec §2): the type seeds DEACTIVATED and an
   admin explicitly activates it — trusted-editor escape hatch, never sanitized
   (sanitizing would make it rich_text). #}
<div class="thallo-block thallo-block-html{{ style_classes('root') }}"{{ style_attrs('root') }}>{{ data.code|default('')|raw }}</div>
