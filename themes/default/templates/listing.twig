{% extends 'layout.twig' %}
{% block title %}{{ type_name|default(type) }} — {{ site.name }}{% endblock %}
{% block content %}
  <h1>{{ type_name|default(type) }}</h1>
  {% include '_listing_rows.twig' %}
  {% include '_pagination.twig' %}
{% endblock %}
